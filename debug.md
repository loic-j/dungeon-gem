# Debug

## Hide UI overlay (show 3D scene only)

Open browser console and run:

```js
// Hide all overlay UI
document.getElementById("ui").style.display = "none";

// Restore
document.getElementById("ui").style.display = "";
```
